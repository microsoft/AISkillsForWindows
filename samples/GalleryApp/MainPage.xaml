<Page
    x:Class="GalleryApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GalleryApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="70*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!--Skeletal Skill Thumbnail-->
            <Grid Grid.Column="1" Background="LightGray" Margin="25" Height="400" 
                  Width="400" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Image Source="./Assets/SkeletalDetectorSamplePhoto.jpg"/>
                <Grid Height="150" Width="400" VerticalAlignment="Bottom">
                    <Rectangle Height="200" Width="400" VerticalAlignment="Bottom"
                                                       Fill="Gray" Opacity="0.75"/>
                    <TextBlock Text="Skeletal Detector" 
                               Style="{StaticResource SubheaderTextBlockStyle}"
                               Foreground="White"
                               Padding="10, 0, 0, 0" />
                    <TextBlock Text="A set of sample apps that can detect and identify the poses of the bodies of individuals in a video or image." 
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="White"
                               Padding="10, 60, 0, 0"
                               TextWrapping="Wrap"/>
                </Grid>
                <Button Height="400" Width="400" Opacity="0" Click="NavigateToSkillPage"/>
            </Grid>

            <!--Template-->
            <!--<GridView Grid.Column="1">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Style.Setters>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Grid Background="LightGray" Margin="25" Height="400" Width="400" 
                                          Padding="0, 0, 0, 0">
                                            <Grid Height="200" Width="400" VerticalAlignment="Bottom">
                                                <Rectangle Height="200" Width="400" VerticalAlignment="Bottom"
                                                       Fill="Gray" Opacity="0.25"/>
                                                <TextBlock Text="Skeletal Detector" Style="{StaticResource SubheaderTextBlockStyle}"
                                                           Padding="10, 0, 0, 0"/>
                                                <TextBlock Text="Description..." Style="{StaticResource BodyTextBlockStyle}"
                                                           Padding="10, 60, 0, 0"/>
                                            </Grid>
                                            <Button Height="400" Width="400" Opacity="0" Click="NavigateToSkillPage"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style.Setters>
                    </Style>
                </GridView.ItemContainerStyle>

                <GridViewItem />

            </GridView>-->

        </Grid>
    </Grid>
</Page>
